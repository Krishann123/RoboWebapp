---
import Layout from '../../../layouts/Layout.astro';
const { countryData } = Astro.locals;
if (!countryData) { return Astro.redirect('/404'); }
---
<Layout title={`Webinars - ${countryData.name}`}>
    <div class="container">
        <h1>Webinars in {countryData.name}</h1>
        <div class="list">
            {countryData.webinars.map(item => (
                <div class="card">
                    <h2><a href={`/country/${countryData.slug}/webinars/${item.slug}`}>{item.title}</a></h2>
                    <p>Date: {new Date(item.date).toLocaleDateString()}</p>
                </div>
            ))}
        </div>
    </div>
</Layout> 